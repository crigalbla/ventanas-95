---
import Layout from "@/layouts/Layout.astro"
import Main from "@/components/Main.svelte";
import MainTouchableDevice from "@/components/MainTouchableDevice.svelte";
import { isMobileOrTablet } from "@/utils";
---

<Layout title="Ventanas 95">
	{isMobileOrTablet() ? <MainTouchableDevice client:visible /> : <Main client:visible />}
</Layout>
